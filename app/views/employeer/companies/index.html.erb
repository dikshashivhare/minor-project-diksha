<div class="jumbotron">
  <h1 class="display-4">Earn While Learn</h1>
  <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
  </p>
</div>
<div class="container">  
  <p id="notice"><%= notice %></p>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Contact</th>
        <th>Employeer</th>
        <th colspan="3">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @companies.each do |company| %>
        <tr>
          <td><%= company.name %></td>
          <td><%= company.email %></td>
          <td><%= company.contact %></td>
          <td><%= company.employeer.full_name %></td>
          <td>
            <%= link_to employeer_company_path(company),class:"btn btn-info" do %>
             <i class="fa fa-eye"></i> 
              <% end %>
            <%= link_to  edit_employeer_company_path(company),class:"btn btn-warning" do %>
            <i class="fa fa-pencil-square-o" ></i> 
              <% end %>
            <%= link_to  employeer_companies_path, method: :delete, data: { confirm: 'Are you sure?' } ,class:"btn btn-danger" do%>
            <i class="fa fa-trash" ></i> 
             <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to new_employeer_company_path, class: "btn btn-primary" do %>
    <i class="fa fa-plus"></i> Add New Company
  <% end %>
</div>