
<p id="notice"><%= notice %></p>

<h1>Job Categories</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Icon url</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_categories.each do |job_category| %>
      <tr>
        <td><%= job_category.name %></td>
        <td><%= job_category.icon_url %></td>
        <td><%= job_category.description %></td>
        <td><%= link_to 'Show', admin_job_category %></td>
        <td><%= link_to 'Edit', edit_admin_job_category_path(job_category) %></td>
        <td><%= link_to 'Destroy', admin_job_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Job Category', new_job_category_path %>
